pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'

                archiveArtifacts artifacts: '**/*.jar', fingerprint: true
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'

                junit '**/target/*.xml'
            }
        }

    }
}